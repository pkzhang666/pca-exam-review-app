import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pengkun.pcaexamreview',
  appName: 'PCA Exam Review',
  webDir: 'dist',
};

export default config;
